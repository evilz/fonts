# Fonts

Petit dépôt de polices + une page HTML (`index.html`) pour les prévisualiser rapidement.

## CI

Une vérification GitHub Actions exécute **HTMLHint** sur `index.html` afin d’éviter les erreurs de base.
